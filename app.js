import { handleMousedown, handleMousemove, handleMouseup } from './js/events.js';

const menu = document.getElementById('menu');
menu.addEventListener('click', () => {
  console.log('coucou');
  // displays the modal to be able to input the words wanted
});